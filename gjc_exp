当前版本不会把分析结果默认存到项目目录。

默认情况下，结果会落到当前用户 Documents 目录下：

C:\Users\你的用户名\Documents\SportsAnalysis

目录结构示例：

Documents/
└─ SportsAnalysis/
   └─ push_up/
      └─ 20260405_153210/
         ├─ frame_metrics.csv
         └─ session_summary.json

如果需要改到别的位置，可以修改：

app/config.py

中的：

output_dir

例如：

output_dir = r"D:\SportsAnalysisData"
1. 放到 motions/
2. MotionRegistry.load_all() 启动时读它
3. 解析成 MotionDefinition
4. Pipeline 根据 motion_id 拿到它
5. MotionAnalyzer 和 RuleEngine 使用它